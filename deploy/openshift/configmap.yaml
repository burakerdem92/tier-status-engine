apiVersion: v1
kind: ConfigMap
metadata:
  name: tier-status-engine
data:
  DB_URL: jdbc:postgresql://postgresql:5432/tierstatus
  KAFKA_BOOTSTRAP_SERVERS: kafka:9092
  KAFKA_PARTITIONS: "48"
  OUTBOX_BATCH_SIZE: "250"
